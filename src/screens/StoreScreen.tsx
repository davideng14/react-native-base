import React from 'react';
import { View, Button } from 'react-native';

export default function StoreScreen() {
    return (
    <View>
        <Button title={"StoreScreen"} onPress={() => {
            return true;
        }} />
    </View>
    )
} 